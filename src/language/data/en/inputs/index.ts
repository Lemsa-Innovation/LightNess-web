import { Inputs } from "@/language/structure/inputs";

export const inputs: Inputs = {
  // Only basic input translations for reset password functionality
  commons: {
    name: {
      label: "Name",
      placeholder: "Enter name",
    },
  },
};
